<div class="m-lightbox {class}" r-hide={!visible}>
    <div class="lightbox_dialog">
        <h3 class="lightbox_title">{title}</h3>
        <a class="lightbox_close" on-click={this.close()}></a>
        {#inc @(contentTemplate)}
    </div>
</div>
